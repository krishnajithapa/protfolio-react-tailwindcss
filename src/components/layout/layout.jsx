const Layout = ({ children }) => {
  return <div className=" bg-green-50 p-6">{children}</div>;
};

export default Layout;
// This component serves as a layout wrapper for the body content of the application.
// It applies padding and a background color to the content passed as children.
